[build]
  command = "npm run build"
  publish = "dist"

# Production branch
[context.production]
  branch = "main"

# Staging branch - auto-deploys to staging--yoursite.netlify.app
[context.staging]
  command = "npm run build"

# Enable branch deploys for staging
[context.branch-deploy]
  command = "npm run build"
